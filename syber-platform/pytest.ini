[pytest]
# The spec names the security batteries runner.py / rag_sim.py (§15), which do
# not match pytest's default test_*.py discovery. Broaden discovery so the full
# suite (component + injection + poisoning) is collected by `pytest tests`.
python_files = test_*.py runner.py rag_sim.py
testpaths = tests
addopts = -q
